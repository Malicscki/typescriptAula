// "compile": "tsc", //quando executado transforma os arquivos TS para JS
// "watch": "tsc -w" //fica online e qualquer arquivo modificado dentro da pasta dist vai ser alterado dentro da JS 
// sempre executar no TERMINAL o comando npm run start (lite server)

ctrl+H localiza e substitui texto


AUTO IMPORT
const controller = new NegociacaoController();. Quando eu escrevo, o Visual Studio Code já vai me dá um atalho para importar o negociação-controller.
vai gerar:
import { NegociacaoController } from './controllers/negociacao-controller'; 
controller = new NegociacaoController();

mas precisa colocar js no final, senão não vai funcionar.
import { NegociacaoController } from './controllers/negociacao-controller.js';

ctrl + space pra abrir o autocomplete 
